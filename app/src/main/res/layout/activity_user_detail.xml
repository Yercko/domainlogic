<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <!-- Imagen de Avatar -->
        <ImageView
            android:id="@+id/imageViewAvatar"
            android:layout_width="100dp"
            android:layout_height="100dp"
            android:layout_gravity="center"
            android:scaleType="centerCrop"
            android:src="@drawable/ic_launcher_background" /> <!-- Reemplazar con un placeholder adecuado -->

        <!-- Nombre de Usuario -->
        <TextView
            android:id="@+id/textViewUsername"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:layout_marginTop="16dp"
            android:text="Nombre de Usuario"
            android:textSize="20sp"
            android:textStyle="bold" />

        <!-- ID del Usuario -->
        <TextView
            android:id="@+id/textViewUserId"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:text="ID del Usuario"
            android:textSize="16sp"
            android:layout_marginTop="8dp" />

        <!-- URL del Perfil -->
        <TextView
            android:id="@+id/textViewHtmlUrl"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:text="URL del Perfil"
            android:textSize="16sp"
            android:layout_marginTop="8dp" />

        <!-- Otros detalles -->
        <!-- Puedes añadir más TextViews u otros elementos aquí según sea necesario -->

    </LinearLayout>
</ScrollView>
